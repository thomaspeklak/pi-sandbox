You are the review/fix agent for the agent-sandbox migration.

Review the latest implementation commit and fix issues.

Use these references:
- docs/agent-sandbox-migration/README.md
- docs/agent-sandbox-migration/tasks.md
- docs/agent-sandbox-migration/parity-checklist.md
- docs/agent-sandbox-migration/rust-guidelines.md

Checklist:

1) Review the last commit only (`HEAD`):
   - inspect commit message and changed files
   - verify scope matches selected task(s)
2) Enforce Rust guidelines:
   - file size < 400 LOC
   - no inline tests in production files
3) Run and fix until green:
   - `cargo check`
   - `cargo fmt --all` (apply if needed)
   - `cargo clippy --all-targets --all-features`
   - `cargo test --all-targets --all-features` (last)
4) Fix anything broken or inconsistent (code, docs, checklist status).
5) If a checkbox was marked done incorrectly, correct it.
6) If you made changes, create a follow-up commit with a clear review-fix message.
7) Provide concise summary of issues found + fixes.
8) Stop.
