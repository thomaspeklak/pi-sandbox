[package]
name = "ags"
version = "0.4.0"
edition = "2024"

[dependencies]
dirs = "6"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

[dev-dependencies]
tempfile = "3"
